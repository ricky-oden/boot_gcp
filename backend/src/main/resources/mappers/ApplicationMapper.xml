<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.workflow.mybatis.ApplicationMapper">
    <resultMap id="applicationResultMap" type="com.example.workflow.mybatis.MyBatisApplicationRow">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO applications (title, status, created_at)
        VALUES (#{title}, #{status}, #{createdAt})
    </insert>

    <select id="findById" resultMap="applicationResultMap">
        SELECT id, title, status, created_at
        FROM applications
        WHERE id = #{id}
    </select>

    <select id="findByStatus" resultMap="applicationResultMap">
        SELECT id, title, status, created_at
        FROM applications
        WHERE status = #{status}
        ORDER BY created_at ASC
    </select>

    <update id="updateStatus">
        UPDATE applications
        SET status = #{newStatus}
        WHERE id = #{id}
          AND status = #{expectedStatus}
    </update>
</mapper>
