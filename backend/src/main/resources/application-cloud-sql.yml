spring:
  datasource:
    url: jdbc:postgresql:///${DB_NAME:workflow}?cloudSqlInstance=${INSTANCE_CONNECTION_NAME}&socketFactory=com.google.cloud.sql.postgres.SocketFactory&ipTypes=PUBLIC
    username: ${DB_USER:workflow}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update

