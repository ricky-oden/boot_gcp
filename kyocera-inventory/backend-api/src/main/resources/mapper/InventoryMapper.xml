<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kyocera.inventory.inventory.mapper.InventoryMapper">

    <resultMap id="inventorySearchResultMap"
               type="com.example.kyocera.inventory.inventory.mapper.InventorySearchRow">
        <id property="inventoryId" column="inventory_id"/>
        <result property="itemCode" column="item_code"/>
        <result property="itemName" column="item_name"/>
        <result property="warehouseId" column="warehouse_id"/>
        <result property="warehouseName" column="warehouse_name"/>
        <result property="quantity" column="quantity"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="search" resultMap="inventorySearchResultMap">
        SELECT
            i.inventory_id,
            i.item_code,
            im.item_name,
            i.warehouse_id,
            w.warehouse_name,
            i.quantity,
            i.status
        FROM inventory i
        INNER JOIN item_master im ON im.item_code = i.item_code
        INNER JOIN warehouse w ON w.warehouse_id = i.warehouse_id
        <where>
            <if test="itemCode != null and itemCode != ''">
                AND i.item_code = #{itemCode}
            </if>
            <if test="warehouseId != null">
                AND i.warehouse_id = #{warehouseId}
            </if>
        </where>
        ORDER BY i.inventory_id
    </select>
</mapper>
